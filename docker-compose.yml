services:
  mytodo:
    build: .
    container_name: mytodo
    ports:
      - "3090:3090"
    volumes:
      - ./data:/app/data
      - ./app:/app/app
    environment:
      - TZ=Asia/Shanghai
      - DATA_DIR=/app/data
    restart: unless-stopped
